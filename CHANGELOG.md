# 변경 이력

사용자에게 보이는 변화만 적습니다. 내부 정리는 넣지 않습니다.

---

## 0.15.8 (2026-08-03)

제보받은 일곱 가지에서 시작해, 좌표계를 전수 감사하며 나온 것까지
고쳤습니다. 판정·셀렉트는 그대로입니다.

### 마스크가 사진에 붙어 있습니다

마스크 좌표가 **잘라낸 뒤의 화면** 기준이라, 크롭을 바꾸면 이미 그려 둔
마스크가 사진 위에서 옮겨 갔습니다. 오른쪽 절반을 자르면 왼쪽 위에 찍어 둔
마스크가 사진의 한가운데로 이동하는 식이었습니다.

이제 마스크를 자르기 **전에** 겁니다. 크롭·회전·반전이 마스크를 함께
데려가고, 잘려 나간 곳의 마스크는 같이 사라집니다.

**마스크를 보거나 만지는 동안은 화면이 사진 전체를 보여 줍니다** — 크롭
편집 때 전체가 보이는 것과 같은 방식입니다. 좌표가 전부 사진 전체 기준이
됐으므로, 잘린 화면 위에서 그리면 그리는 순간부터 어긋나기 때문입니다.
조작점을 내려놓으면 잘린 화면으로 돌아옵니다.

연출 비네팅은 반대로 **최종 구도**에 붙습니다 — 크롭을 바꿔도 언제나 잘린
화면의 네 귀퉁이가 어두워집니다. 워터마크·정보 띠와 같은 성격입니다.

### 마스크를 잡는 순간 낡은 최종 미리보기가 화면을 덮던 것을 고쳤습니다

마스크를 만지는 동안은 화면이 사진 전체를 보여 줍니다(위). 그런데 그
전환 직전에 **최종 미리보기가 돌고 있었다면** 그것은 잘린 화면으로 만든
그림이라, 몇 초 뒤 도착하면서 조작점 밑을 덮었습니다. 좌표가 사진 전체
기준으로 통일된 의미가 그 몇 초 동안만 무너지는 셈이었습니다.

이제 표시 기준이 바뀌면 돌고 있던 최종 미리보기를 접고 새로 그립니다.
슬라이더를 움직였을 때 이미 그렇게 동작하던 것을, 화면 기준이 바뀌는
전환에도 적용한 것입니다. 크롭 편집을 열고 닫을 때도 같습니다.

### 크롭·회전한 컷에서도 초점·얼굴 표시가 보입니다

지금까지는 크롭이나 회전이 걸리면 초점 영역(초록)·얼굴 상자·눈 윤곽·AF
지점이 **통째로 사라졌습니다** — 좌표가 어긋나느니 숨기는 쪽이었습니다.
이제 표시 좌표를 크롭·회전·수평보정에 맞춰 옮겨 그립니다. 잘려 나간
상자만 빠지고 나머지는 제자리에 보입니다.

확대한 채 최종 미리보기를 켰을 때 마스크가 옮겨 가던 것도 고쳤습니다 —
마스크가 있는 컷은 보이는 영역만 현상하는 지름길을 타지 않습니다.

### Full Render가 화면과 다르던 것

선명도·텍스처·필름 그레인의 크기가 **화소 단위로 고정**이라, 화면(1400px)과
Full Render(창 크기)와 내보내기(원본 크기)에서 각각 다르게 걸렸습니다. 화면이
가장 작으니 가장 선명해 보이고, 내보낸 파일이 가장 흐렸습니다.

이제 셋 다 그림 크기에 맞춰 걸립니다 — 화면에서 맞춘 것이 그대로 나옵니다.
실측 차이(1400px 대 2800px, 같은 크기로 견줌):

| | 예전 | 지금 |
|---|---|---|
| 텍스처 +50 | 1.13레벨 | **0.15** |
| 선명도 +80 | 1.51 | **0.17** |
| 그레인 +50 | 6.27 | **0.23** |

0.15는 아무 보정도 안 걸었을 때의 값입니다. 즉 차이가 사라졌습니다.

**확대한 최종 미리보기**도 같은 눈금을 씁니다. 보이는 영역만 잘라
현상하는 지름길이 조각을 사진 전체로 알고 반지름을 계산해서, 하필 선명도를
확인하려고 확대한 화면이 내보내기보다 약하게 걸려 있었습니다(실측 6.82레벨
→ 0.13, 기준선 수준).

**기존 편집물의 내보내기 결과가 지금보다 선명해집니다.** 화면에서 보시던
쪽으로 맞춘 것입니다.

크롭이나 마스크를 **편집하는 동안** 최종 미리보기가 도착하면 화면이 잘린
프레임으로 튀던 것도 고쳤습니다 — 이제 편집 중에는 최종 미리보기도 편집
화면과 같은 프레임으로 만듭니다.

### 촬영정보 띠

**두 개씩 들어가던 것**을 고쳤습니다. 최종 미리보기를 켜면 띠가 두 번 그려져
세로가 132px 늘고 같은 문구가 두 줄이 됐습니다.

**세로 사진에서 글자가 잘리던 것**도 고쳤습니다. 띠 두께가 사진의 세로
길이에 비례했는데, 글자는 가로로 흐릅니다. 그래서 세로 사진일수록 띠가
두꺼워지고 글자가 커져서 정작 담기는 정보가 줄었습니다 — 세로 사진에서 여섯
항목 중 둘만, 좁게 자른 사진에서는 기종명 하나만 남았습니다. 이제 어느
화면비에서도 여섯 항목이 다 들어갑니다. 가로 3:2 사진은 예전과 같습니다.

### 프리셋이 그 컷만의 값을 담지 않습니다

크롭·수평 보정·회전이 프리셋에 저장되고 있었습니다. 다른 사진에 불러오면
맞춰 주는 것이 아니라 피사체를 잘라내고 그만큼 기울입니다 — 일괄 적용이
이미 도형을 빼고 있었는데 프리셋만 빠져 있었습니다.

**워터마크는 따로 저장합니다.** 색보정과 성격이 다르고, 같은 워터마크를 여러
색감에 얹거나 같은 색감에 다른 워터마크를 얹는 일이 흔합니다. 워터마크 칸에
자체 프리셋 목록이 생겼습니다.

프리셋을 불러도 지금 잡아 둔 크롭·수평·워터마크·마스크는 그대로 있습니다.

같은 원칙이 새는 곳이 두 군데 더 있었습니다. **모든 컷에 적용**이 다른
컷의 크롭은 지키면서 그 컷에 그려 둔 마스크를 지웠고(일괄 적용 한 번에
국소 보정이 전부 사라졌습니다), **대기열에서 프리셋을 바꾸면** 항목에 걸어
둔 워터마크가 조용히 꺼졌습니다. 둘 다 보정창과 같은 규칙을 따릅니다.

---

## 0.15.7 (2026-07-29)

현상이 **어디에서** 계산하는지를 맞춘 판입니다. 판정·셀렉트는 그대로입니다.

### 확대한 채로 Full Render를 켜면 그림이 휘던 것을 고쳤습니다

확대하면 보이는 영역만 현상해 시간을 아끼는데(4배에서 3.4초 → 0.22초),
그 자르기가 **렌즈 보정보다 먼저** 일어나고 있었습니다. 왜곡·비네팅·색수차는
화면 중심과 크기를 기준으로 정의되므로, 잘라낸 조각을 넘기면 그것을 프레임
전체로 알고 계산합니다.

실측(A6700 + E PZ 16-50mm, 가운데 40%): 등배와 **평균 25.9레벨** 어긋났고
화소의 71.9%가 5레벨을 넘었으며, 조각 모서리는 52.7레벨 밝았습니다. 중앙만
맞아서 알아채기 어려운 종류였습니다.

이제 자르기 전에 렌즈 보정을 겁니다. 확대 결과가 등배와 **한 레벨도 다르지
않습니다**(0.00). 시간을 아끼는 것은 그대로입니다.

### 밝은 부분이 더 이상 뭉개지지 않습니다

우리 중립 렌더는 카메라 JPEG보다 어둡습니다(실측 +0.77~+2.82EV). 그만큼
노출을 올리면 넘친 빛이 천장에 붙어 **밝은 면이 납작해졌습니다.** 카메라는
같은 밝기에 어깨로 도달하므로 그렇지 않습니다.

이제 천장에 다가가는 빛을 압축합니다. 노출을 올려 카메라 밝기에 맞췄을 때
255에 붙는 화소:

| 컷 | 올린 노출 | 예전 | 지금 | 카메라 JPEG |
|---|---|---|---|---|
| 소니 A6700 | +0.77 | 0.77% | **0.06%** | 0.17% |
| 소니 A6700 | +0.97 | 0.54% | **0.00%** | 0.00% |
| 파나소닉 S5M2X | +1.72 | 1.48% | **0.00%** | 0.00% |
| 파나소닉 S5M2X | +2.82 | 3.19% | **0.00%** | 0.00% |

밝은 영역에 남는 고유 레벨은 그대로입니다(144 → 144) — 계조를 뺏지 않고
뭉개짐만 없앱니다. 그림 전체 변화는 0.02~0.18레벨입니다.

**순백은 순백으로 남습니다.** 노출을 얼마나 올리든 흰 벽은 255입니다.
빛의 양이 어깨가 시작되는 지점(최상단 0.23스톱) 아래면 한 톨도 달라지지
않습니다 — 노출 슬라이더에 찍히는 값은 여전히 정확한 물리량입니다.

더 세게 거는 값도 만들어 견줬는데 이득이 없었습니다. 압축을 두 배 넓게
시작하면 클리핑 제거 효과는 같으면서 밝은 영역 고유 레벨이 254에서 247로
줄고, 밝은 면 전체가 눈에 띄게 눌립니다. **필요한 만큼만 겁니다.**

### 넓은 색공간에서 보정합니다

카메라→작업공간 변환은 LibRaw 안에서 일어납니다. 지금까지 그 목적지가
sRGB라, **우리 코드가 화소를 보기도 전에** 담기지 않는 색이 잘렸습니다.
실측한 한 컷에서 화소의 0.79%가 그렇게 사라졌고, 그중 88%가 주황·노랑이며
가장 큰 덩어리가 24,478화소였습니다 — 조명이 센 면 하나가 통째로 뭉개진
것입니다.

이제 ProPhoto에서 받습니다. 예전에 죽던 화소의 **72~98%가 살아납니다.**

겉보기는 그대로입니다 — 화면과 저장은 sRGB로 옮겨 나가고, 무채색은
0.01~0.03레벨, 전체 평균은 0.22~0.36레벨만 움직입니다.

### 화이트밸런스를 센서 값에서 겁니다

화이트밸런스는 물리적으로 센서의 선형값에 거는 연산입니다. 지금까지는 이미
현상된 값에 채널 게인을 곱했는데, 그것은 as-shot에서 멀어질수록 벌어지는
근사입니다:

| as-shot에서 이동 | 평균 오차 | 5레벨 넘게 갈리는 화소 |
|---|---|---|
| 미세 조정 | 0.95~1.91레벨 | 0.2% |
| 3400K → 5500K | 5.94 | 48~56% |
| 3400K → 9000K | **9.22~9.39** | **64~70%** |

이제 색온도를 바꾸고 손을 떼면 그 색온도로 다시 현상해 **오차가 0이
됩니다.** 슬라이더를 끄는 동안에는 예전 방식으로 미리 보여 조작감을
지킵니다(재현상이 40배 느립니다). 저장된 색온도가 있는 컷은 처음부터 그
색온도로 엽니다.

**내보내기는 언제나 정확한 쪽으로 갑니다** — 속도를 다투지 않기 때문입니다.
색조(tint)는 그대로 표시값 위의 조정으로 남습니다.

### 기종 보정을 저장해 둔 분은 다시 측정해 주십시오

기종 보정은 우리 현상 결과와 카메라 JPEG을 견줘 얻은 채널 배율입니다. 견주는
공간이 넓어졌으므로 예전 값은 뜻이 조금 달라졌습니다.

실제 차이는 작습니다 — 캐논 R6 Mark III 10장으로 다시 재 보니 채널당 최대
1.6%였고, 장마다 흔들리는 폭(2~3%)보다 작습니다. 무채색은 두 공간에서 같은
숫자라 그렇습니다. 그래도 보정 창에서 한 번 다시 재시는 편이 정확합니다.

**보정을 저장한 적이 없으면 해당 없습니다.** 그 경우 LibRaw이 기종 정보를
직접 써서 변환하므로 낡을 값이 없습니다.

---

## 0.15.6 (2026-07-28)

현상이 다루는 숫자가 무엇을 뜻하는지 맞춘 판입니다. 판정·셀렉트는
그대로입니다.

### 노출이 이제 물리적으로 정확합니다

0.15.5에서 노출을 "빛의 양에 거는" 연산으로 고쳤는데, **되돌리는 곡선이
틀려 있었습니다.** 노출은 `디코더 감마 → 기종 보정 → 표준 프로파일 곡선`을
지난 값 위에 걸리는데, sRGB 곡선으로 되돌리고 있었습니다.

이제 그 셋을 합성한 곡선으로 되돌립니다. 카메라가 실제로 한 스톱 더 받은
결과(LibRaw이 센서 선형에서 계산)와 대조한 +2.0EV 화소 오차:

| 카메라 | 예전(sRGB 가정) | 지금(합성 역산) |
|---|---|---|
| 소니 A6700 | 11.65 | **1.61** |
| 파나소닉 S5M2X | 8.78 | **0.19** |
| 캐논 R6 Mark III | 13.96 | **0.98** |

디코더 감마만 맞추는 것으로는 **오히려 나빠집니다**(19.18·10.20·20.86) —
프로파일 곡선이 지배하기 때문입니다. 이 결과 때문에 색공간 선택 기능은
접었습니다. 전달함수를 바꿔도 노출 정확도에는 도움이 되지 않습니다.

**JPEG·HEIF 원본은 따로 다룹니다.** 디모자이크도 프로파일도 지나지 않은
진짜 sRGB라 그쪽 곡선으로 되돌립니다 — 같은 +1EV가 원본 종류에 따라 다른
양의 빛을 뜻하면 안 됩니다. 국소 보정(마스크)의 노출도 전역과 같은 곡선을
씁니다.

**노출을 준 컷은 결과가 달라집니다.** 예전보다 정확해지는 방향이고, 그
폭은 위 표대로 컷당 평균 9~14레벨입니다.

**카메라 JPEG에 맞추기**도 같은 곡선을 씁니다. 이 기능이 노출을 찾을 때는
아직 sRGB 곡선으로 되돌리고 있어서, 엔진이 거는 것과 다른 공간에서 계산한
값이 슬라이더에 찍혔습니다. 그림은 뒤따르는 톤 곡선이 흡수해 거의 같았지만
(루마 오차 10.17 → 10.15), 이제 **엔진이 +1EV를 건 그림을 정답지로 주면
피팅이 +1EV를 되찾습니다**(예전 식은 0.84를 냈습니다).

**자동 렌즈 비네팅**도 마찬가지였습니다. 0.15.5에서 "표시값이 아니라 빛에
걸어야 한다"까지는 고쳤는데, 되돌리는 곡선이 sRGB였습니다. 구석 잔차가
중앙 밝기에 따라 −0.36 ~ +0.21스톱으로 **부호까지 뒤집혔습니다** — 하필
비네팅이 펴려는 그 밝기 기울기 위에서 어긋납니다. 균일한 장면을 넣고 재면
평탄도 오차가 1.57레벨(최대 3.44)이었고, 이제 **0.00**입니다. 실사진에서는
구석이 평균 2.2레벨(최대 24.8) 밝아지고 중앙은 그대로입니다.

이 넷이 같은 실수였습니다. 빛으로 되돌리는 수식이 여러 군데에 각각 있었고
그중 셋이 낡았습니다. 이제 변환은 한 쌍뿐이고 노출·피팅·비네팅이 모두
그것을 씁니다.

### 수동 비네팅 슬라이더가 눈금대로 움직입니다

**같은 슬라이더 값이 밝기마다 다른 뜻이었습니다.** 슬라이더 25가 어두운 곳
(레벨 40)에서는 +0.43스톱, 밝은 곳(레벨 190)에서는 +1.16스톱이었습니다. 한
장 안에서도 갈려서, 어두운 구석을 보고 맞추면 밝은 구석이 터졌습니다 —
실사진 슬라이더 50에서 날아간 화소가 **1.68%**(원본 0.08%)였습니다.

이제 빛의 양에 걸어서 밝기와 무관하게 같은 양이 걸립니다(폭 0.00). 같은
사진에서 날아간 화소가 **0.26%**로 줄었습니다.

**슬라이더가 스톱에 선형입니다.** 예전에는 앞쪽 절반이 전체 효과의 60%를
해서 미세 조정이 어려웠습니다. 이제 50이 정확히 100의 절반이고, 100은 구석을
**+2.2스톱** 올립니다 — 자동 프로필이 실제로 거는 양(실측 +2.14스톱)까지
닿아야 프로필 없는 렌즈에서 대체재가 되기 때문입니다.

**기존에 이 슬라이더를 쓴 컷은 결과가 달라집니다.** 값의 뜻 자체가 바뀌었으니
다시 맞춰야 합니다.

### Adobe RGB로 찍은 JPEG·HEIF를 제대로 엽니다

지금까지 모든 JPEG·HEIF를 sRGB로 간주했습니다. Adobe RGB로 찍은 파일은
보정창에서 **채도가 빠져 보이고**(화면은 무엇이든 sRGB로 그립니다) 노출도
어긋난 곡선으로 계산됐습니다.

이제 원본의 색공간을 읽어 작업 공간으로 옮겨 놓습니다. 색 관리 도구가 하는
변환과 대조하면 **평균 0.47레벨**(최대 2)로 일치합니다. sRGB 원본은 한
화소도 건드리지 않습니다.

판별은 EXIF의 Interoperability 표시(`R03`=Adobe RGB, `R98`=sRGB)로 하고,
그 표시가 없으면 ColorSpace 태그로 물러섭니다 — 실측한 보관함 125개 폴더 중
**19개**가 그런 경우였습니다. **Adobe RGB는 ColorSpace가 2가 아니라
65535(Uncalibrated)로 나오므로** 그 태그만 보면 구분되지 않습니다.

**폴더가 아니라 파일마다 봅니다.** 보관함 네 곳 125개 폴더를 훑어 보니 Adobe
RGB가 6개 폴더 약 5,800장이었고, 그중 하나는 2,159장 가운데 마지막 2장만
sRGB였습니다 — 촬영 도중 설정을 바꾼 것입니다. 폴더 단위로 처리했다면 그
2장이 조용히 틀렸을 자리입니다. 파일명의 밑줄 접두사(DCF의 Adobe RGB 표시)도
믿지 않습니다. 6개 폴더 중 밑줄이 붙은 것은 1개뿐이었습니다.

모르는 파일은 sRGB로 봅니다. 멀쩡한 sRGB를 Adobe RGB로 잘못 보면 채도를 깎게
되어 손해가 반대 방향보다 큽니다.

RAW는 해당하지 않습니다 — 센서 데이터를 직접 현상하므로 카메라의 색공간
설정과 무관합니다. 셀렉트 화면의 판정도 그대로입니다(이득이 작은 반면
분석 캐시를 통째로 갈아야 합니다).

### 내보낼 색공간을 고를 수 있습니다 (sRGB / Adobe RGB)

내보내기 창에 **색공간**이 생겼습니다. 기본은 sRGB이고, 인쇄용으로 보낼
파일은 Adobe RGB로 내보낼 수 있습니다.

**태그만 붙이는 것이 아니라 화소도 함께 변환합니다.** 하나만 하면 뷰어가
다른 공간의 숫자로 읽어 색이 틀어집니다. 색 관리를 하는 프로그램에서
되돌려 보면 sRGB로 내보낸 것과 **평균 0.52레벨** 차이입니다 — 겉보기가
그대로 보존됩니다.

**파일을 다시 저장하지 않습니다.** 프로파일을 넣겠다고 이미지 라이브러리로
다시 저장하면 JPEG은 재압축되고(실측 14,880 → 6,895바이트, 화소 최대
14레벨 차이) 16비트는 8비트로 떨어집니다. 그래서 인코딩된 바이트는
손대지 않고 세그먼트·청크·태그만 끼워 넣습니다 — JPEG·PNG·TIFF 모두
화소가 원본과 **완전히 같습니다.**

WebP는 프로파일을 넣을 자리가 없어 sRGB로 잠깁니다. 변환만 하고 태그를
못 붙이면 색이 틀어진 파일이 나가기 때문입니다.

Adobe RGB 프로파일은 공개 규격의 프라이머리·감마로 직접 만듭니다(어도비가
배포하는 프로파일 파일은 재배포 조건이 있어 담지 않습니다). 그래서 이름도
"Compatible with Adobe RGB (1998)"입니다.

### 16비트로 내보낼 수 있습니다

내보내기 창에 **비트 심도**가 생겼습니다. PNG·TIFF에서 16비트를 고를 수
있고, JPEG·WebP는 형식 자체가 8비트만 담아 잠깁니다(잠글 때 이유를 함께
적습니다).

보정 파이프라인은 원래 float으로 흐르고 있었습니다 — 실측으로 디모자이크
직후 채널당 계조가 **544만 단계**(8비트 상한은 256)이고, 노출·곡선·채도·
노이즈·명료도를 지나도 477만~604만이 남습니다. 지금까지는 마지막에 8비트로
떨구느라 그 계조를 버리고 있었습니다.

**8비트 결과는 한 화소도 달라지지 않습니다.** 16비트로 내보낸 뒤 8비트로
떨궈 보면 8비트 파일과 **0레벨 차이**입니다 — 같은 계산이고 마지막
양자화만 다릅니다.

### HSL과 언저리 제거가 계조를 죽이던 것을 고쳤습니다

위 작업 중에 찾았습니다. **HSL 색상대 조정**과 **보라·녹색 언저리 제거**만
8비트로 왕복하고 있어서, 그 둘을 건드리면 그 자리에서 계조가 256단계로
떨어졌습니다(실측: 통과 후 고유 레벨 544만 → 256, 언저리 제거는 228).
16비트로 내보낼 때 "HSL을 만지면 계조가 죽는" 조용한 함정이었을 것입니다.

이제 float으로 계산합니다. 결과는 사실상 그대로입니다 — 8비트로 떨궈
예전 구현과 대조하면 평균 0.24~0.77레벨 차이이고, 그마저도 대부분 예전
코드의 **버림**(반올림이 아님) 때문입니다. 여덟 색상대가 전부 제자리에
반응하는 것도 합성 색상환으로 확인했습니다(빨강 0°, 파랑 240° …).

### 파나소닉 RW2를 제대로 재는 선택지

**RW2는 지금까지 다른 자로 재고 있었습니다.** 내장 프리뷰가 센서 긴 변의
**32%**밖에 안 되기 때문입니다(실측 DC-S5M2X: 센서 6008×4008, 프리뷰
1920px). 소니 ARW·캐논 CR2/CR3는 98~99%라 해당하지 않습니다. 선명도를
원본 해상도에서 잰다는 것이 판정의 전제인데, 그 전제가 파나소닉에서만
깨져 있었습니다.

분석 시작 창에 **프리뷰가 작은 RAW는 현상해서 분석**이 생겼습니다(기본
꺼짐). 해당 파일이 있는 폴더에서만 나오고, 몇 장인지 함께 적습니다.
켜면 그 컷들을 현상해서(긴 변의 50%) 잽니다.

느려지는 만큼을 **두 경우의 예상 시간으로 나란히** 보여 줍니다. 실측
(DC-S5M2X, 워커 12) 장당 15.9 → 79.9ms입니다.

실파일 검증에서 RW2 선명도가 10~47% 내려갔고(작은 프리뷰는 축소 때문에
더 선명해 보입니다), **소니 ARW는 소수점까지 그대로**였습니다. 옵션마다
캐시가 따로 쌓이므로 되돌리면 예전 결과가 그대로 다시 나옵니다. **끈
상태의 캐시 지문은 예전과 같으므로, 이 옵션을 쓰지 않으면 다시 분석하지
않습니다.**

'작다'는 판단은 확장자가 아니라 파일마다 프리뷰와 센서를 직접 비교해
합니다 — 목록에 없는 기종이 같은 사정이어도 함께 구제됩니다.

### 하이라이트 복원 — 보정 옵션

기본 패널 맨 아래에 **하이라이트 복원 (RAW)**(기본 끔)이 생겼습니다.
센서에서 포화한 자리를 아직 남아 있는 채널로 재구성합니다 — 무대 LED와
스포트라이트가 흰 덩어리 대신 구조를 유지합니다(실측 A6700 콘서트 컷:
하이라이트에서 평균 50레벨 차이). 콘서트 보관함 표본 48장 중 25장에
센서 포화가 실제로 있었습니다.

켜면 **전체가 1~1.5스톱 어두워집니다.** 복원 방식(LibRaw blend)이
하이라이트가 들어갈 헤드룸을 확보하기 때문이고, 몰래 되올리면 그 헤드룸이
도로 잘려 의미가 없어집니다. 노출 슬라이더를 취향껏 올리십시오 — 0.15.5
부터 노출이 빛의 양에 걸리므로 정확한 역연산이고, 그때 하이라이트는
잘리는 대신 부드럽게 말립니다. JPEG·HEIF는 되살릴 센서 채널이 없어
잠깁니다. 켜고 끌 때 미리보기를 다시 현상합니다(1~2초).

### HIF를 10비트 그대로 보정합니다

지금까지 소니 HIF의 10비트를 8비트로 눌러 받아 보정 관용도를 잃고
있었습니다. 이제 보정 출발점이 원래 비트 깊이로 옵니다 — 실측
(DSC02290.HIF): 채널당 계조 256단계 → **1,024단계**. 노출을 크게 올려도
계조가 네 배 늦게 끊깁니다. 셀렉트 판정·썸네일 경로는 그대로 8비트라
분석 결과와 캐시는 변하지 않습니다.

---

## 0.15.5 (2026-07-27)

현상 엔진에서 물리적으로 광량에 걸어야 할 연산 둘이
감마가 걸린 표시값에 걸려 있던 것을 고칩니다. 판정·셀렉트는 그대로입니다.

### 노출 슬라이더가 하이라이트를 1.1스톱 일찍 날려 먹던 것을 고쳤습니다

**노출을 빛의 양이 아니라 화면에 찍을 값에 곱하고 있었습니다.** 보정
파이프라인이 다루는 0~255는 감마가 걸린 표시값인데, 거기에 2^EV를 그대로
곱했습니다. 그래서 +2.0EV를 주면 **레벨 64(25% 회색) 위가 전부 순백**이
됐습니다. 물리적으로는 레벨 137부터 포화하는 것이 맞습니다.

같은 컷에 같은 +2.0EV를 줬는데 다른 현상 프로그램은 멀쩡하고 우리만
하이라이트가 다 날아간다는 제보로 드러났습니다. 실측(파나소닉 GX 계열
RW2, +2.0EV): **세 채널이 모두 포화해 정보가 사라진 화소가 25.7% →
3.3%로** 줄었습니다. 벽돌·창틀처럼 원래 살아 있어야 할 중간 밝기가
순백으로 뭉개지던 것이 살아납니다.

RAW를 덜 읽고 있던 것은 아니었습니다 — 이 파일은 센서 포화 화소가 0개로,
화이트 레벨(16319) 아래에 여유가 남은 상태였습니다.

### 렌즈 자동 비네팅 보정이 과하게 걸리던 것을 고쳤습니다

같은 부류를 하나 더 찾았습니다. **렌즈 주변부 어두워짐을 되돌리는 보정도
빛의 양이 아니라 화면에 찍을 값에 곱하고 있었습니다.** 비네팅은 렌즈가
빛을 깎은 것이라 되돌리는 배수도 광량에 걸어야 하는데, 감마가 걸린 값에
걸리면서 실효 배수가 크게 부풀었습니다.

합성 검증에서 **보정한 쪽이 보정하지 않은 쪽보다 더 어긋났습니다** —
주변부를 되살리는 대신 반대로 밝게 밀어 버렸습니다(평탄도 오차 20.9 →
31.1, 제대로 걸면 0.0). 렌즈 프로필 표본 58개에서 구석 과보정이 중앙값
**+1.50스톱**이었습니다.

프로필에 비네팅 계수가 있는 렌즈에서만 나타납니다(내장 DB 1,218개 중
511개, 42%). 계수가 없는 렌즈는 원래 보정이 걸리지 않아 그대로입니다.
**설정 → 광학의 자동 보정을 켜 두고 찍은 컷은 주변부가 달라 보입니다** —
그동안 하얗게 뜨던 네 귀퉁이가 제자리로 돌아옵니다.

덤으로 정밀도도 살렸습니다. 예전에는 이 단계에서 8비트로 떨궈, 디모자이크가
넘겨 준 계조를 비네팅 보정 하나 때문에 잃고 있었습니다.

**같은 노출 숫자가 이제 덜 밝게 그려집니다.** 예전 값으로 맞춰 둔 편집물과
프리셋은 다시 손봐야 합니다 — 특히 노출을 크게 올려 둔 컷일수록 차이가
큽니다(+0.5EV에서 0.27스톱, +3EV에서 1.64스톱). 국소 보정(마스크)의 노출도
같이 바뀝니다. **카메라 JPEG에 맞추기**는 같은 수식으로 다시 맞추므로
그대로 쓰시면 됩니다.

---

## 0.15.4 (2026-07-27)

분석 속도만 손본 판입니다. 기능과 판정은 그대로입니다.

실배치 실측(맥 M1 Pro 16GB, 2,915장 전량 분석): **187.5초, 장당 64ms.**

### 분석이 빨라졌습니다

장면 지문과 썸네일을 원본 프리뷰(6192×4128)가 아니라 **얼굴 검출용으로
어차피 만드는 1024px 축소본**에서 만듭니다. 셋이 제각각 원본에서 줄이고
있었습니다.

실배치 A/B(맥 M1 Pro, 5워커, 서로 겹치지 않는 구역): **13.9 → 20.1장/s
(+44.7%)**. 장당으로는 dhash 10.9→2.7ms, 썸네일 47.0→2.3ms입니다. 윈도우
쪽 실측은 +10%로, 썸네일 저장 비용이 플랫폼마다 크게 달라 이득도 다릅니다.

**판정은 한 픽셀도 바뀌지 않습니다** — 초점 결과 60/60 완전 일치. 장면
지문만 최대 1비트 달라지는데(60장 중 4장) 장면 묶기 임계가 64비트 중
40이라 묶음도 그대로입니다(150장·12그룹 동일).

### 분석 워커가 너무 적게 뜨던 것을 고쳤습니다

램이 넉넉한데도 워커가 두어 개만 떴습니다. 원인이 둘이었습니다.

- **쓸 수 있는 램을 절반으로 또 깎고 있었습니다.** 이 값은 이미 "당장
  되돌려받을 수 있는" 몫이라 다른 프로그램이 쓰는 메모리가 빠져 있는데,
  거기에 0.5를 한 번 더 곱했습니다.
- **파일 형식별로 예산을 가르면서 최악값을 물렸습니다.** 배치에 JPEG이
  한 장만 섞여도 전체가 최악값으로 넘어갔고, 그 값(1,300MB)이 실측
  (형식에 따라 134~546MB)과 크게 어긋나 있었습니다.

16GB 맥에서 JPEG·HEIF 폴더가 **워커 2개 → 7개**가 됩니다. 형식별 분기는
없앴습니다 — 어떤 폴더든 같은 기준으로 정합니다.

너무 많이 띄우는 쪽도 막아 둡니다. 워커당 메모리가 가장 큰 HEIF에서,
코어 수만큼(9개) 띄우면 스왑이 시작되며 그만큼 느려집니다.

### 올려 쓸 때 — 폴더를 처음 열 때 한 번 다시 분석합니다

지문을 뜨는 방식이 바뀌어 알고리즘 버전이 **5**로 올라갔습니다. 한 폴더
안에 옛 방식과 새 방식의 지문이 섞이면 장면 묶기가 두 방식을 오가며
비교하게 되므로 캐시를 갈아 끼웁니다. 따로 하실 일은 없고, 다시 분석하는
그 작업 자체가 위 속도 개선을 받습니다.

---

## 0.15.3 (2026-07-26)

### 세로로 찍은 컷이 180° 돌아간 채 판정되던 것을 고쳤습니다

**EXIF 방향이 두 번 적용되고 있었습니다.** 이미지를 읽는 OpenCV가 방향
태그를 이미 적용해 주는데, 그 위에 방향 보정을 한 번 더 걸었습니다. 그래서
방향 태그가 1이 아닌 컷 — 세로로 세워 찍은 사진 전부 — 이 180° 뒤집힌 채
분석됐습니다.

크기만 보면 "세로 사진이 가로로 나왔네" 정도로 보이지만, 실제로는 **얼굴이
거꾸로 서서 검출이 통째로 실패**합니다(실측: 세로 JPEG에서 얼굴 0개 →
수정 후 1~3개). 얼굴을 못 찾으니 주 피사체·눈 감김·AF 신호가 전부 죽고,
격자 타일 폴백으로 점수가 매겨졌습니다. AF 위치도 엉뚱한 자리에 찍혔습니다.

RAW의 내장 프리뷰와 JPEG·HEIF 두 경로가 같은 원인이었습니다. 소니 배치는
대부분 방향 태그가 1이라 지금까지 드러나지 않았고, 세로 컷이 많은
**캐논·니콘 쪽에서 손해가 컸습니다** — 실측한 보관함에서는 분석된 컷의
33%(36,156장)가 세로였습니다.

**세로로 찍은 컷이 있는 폴더는, 특히 캐논·니콘이라면 다시 분석해
보십시오.** 지금까지의 등급은 얼굴을 못 본 상태에서 매겨진 것이라 다시
분석하면 판정이 달라집니다.

### 외장·NAS에서 장수가 두 배로 세어지던 것을 고쳤습니다

macOS는 exFAT·SMB·NTFS 볼륨에 `._DSC1234.JPG` 같은 **AppleDouble 파일**을
같이 만듭니다. 확장자가 `.JPG`라 사진으로 세어졌고, 이름이 `._`로 시작해
RAW 짝 판정에도 걸리지 않았습니다. 4KB짜리 부속 파일이 전부 한 장으로
세어져 **장수가 정확히 두 배**가 되고, 열리지 않으니 전부 실패로 떨어져
keep 비율이 반토막 났습니다(실측 16.0% → 8.0%). 사용자 외장 볼륨 하나에서
75,840개가 나왔습니다.

이제 이름이 `.`으로 시작하는 숨김 파일은 목록에서 건너뜁니다(`.DS_Store`도
같이 걸러집니다). 외장 디스크나 NAS의 폴더를 열어 왔다면 장수와 keep
비율이 이번 판에서 제자리로 돌아옵니다.

### 짝 파일이 손대지 않은 그대로 나갑니다

**"원본 RAW 복사 끄기 + 짝 파일 포함"** 조합에서 짝 파일(카메라가 같이
뽑아 준 JPEG, `.xmp`)이 복사되지 않고 현상 엔진을 타고 있었습니다. 카메라
JPEG은 손실 재압축되면서 EXIF와 파일 이름이 변형됐고, `.xmp`는 이미지가
아니라 실패로 기록됐습니다.

짝 파일은 이제 **어떤 옵션 조합에서도 바이트 그대로** 복사(또는 이동)
됩니다. "원본 RAW 복사"는 원본 RAW를 남길지 정하는 옵션이지, 사용자가
따로 켠 "짝 파일 포함"을 무르는 스위치가 아닙니다.

### 루페에서 주 피사체를 바꿔도 점수가 배치와 어긋나지 않습니다

미리보기에서 얼굴을 클릭해 주 피사체를 바꾸면 그 자리에서 초점을 다시
재는데, 이때 **배치 분석에 쓴 설정이 하나도 넘어가지 않았습니다.** 선명도
계수·검출 해상도·노이즈 차감이 전부 기본값으로 돌고 AF 정보도 빠져서,
주 피사체만 바꿨을 뿐인데 점수가 배치와 달라지고 "주 피사체 불확실"
표시가 조용히 사라졌습니다. 고치라고 만든 기능이 오히려 판정을 어긋나게
만들고 있었습니다. 이제 배치와 같은 설정으로 다시 잽니다.

### 카메라 룩 매칭 — 보정창을 카메라 JPEG에서 시작합니다

RAW 보정창을 열면 평평한 중립 현상이 나와, 셀렉트할 때 본 카메라 JPEG과
이질감이 컸습니다. 보정 패널 위쪽의 **카메라 JPEG에 맞추기** 버튼을 누르면
RAW에 들어 있는 카메라 JPEG을 정답지 삼아 **노출·톤 곡선·채도를 즉석에서
맞춥니다**(장당 ~16ms). 몰래 거는 것이 아니라 실제 슬라이더 값으로
기록되므로, 그 위에서 계속 손보거나 항목별로 되돌릴 수 있습니다. JPEG·HEIF
원본에서는 맞출 대상이 따로 없으므로 버튼이 잠깁니다.

실파일 31장 검증에서 밝기 오차(루마 MAE)가 19.8에서 6.5로 67% 줄었습니다.
카메라의 국소 톤매핑까지 따라가지는 못하므로 **완벽한 복제가 아니라
시작점**입니다. 디테일·마스크·크롭은 건드리지 않습니다.

설정 → 일반 → 보정의 **보정창을 카메라 룩 시작점으로 열기**(기본 꺼짐)를
켜면 보정창을 열 때마다 자동으로 걸립니다. **이미 손댄 컷은 절대 건드리지
않습니다** — 아직 아무 편집도 없는 RAW에만 적용됩니다.

### 채점표 오른쪽에 촬영 정보가 나옵니다

채점표는 "왜 이 점수인가"를 답하지만 그다음 질문 — "이 컷은 어떤 설정으로
찍었나" — 는 답하지 못했습니다. 채점표 오른쪽 빈자리에 촬영 시각, 바디,
렌즈, 초점거리(크롭 바디는 **35mm 환산**을 함께), 조리개·셔터·ISO,
**AF 영역 모드**, 그리고 값이 있을 때만 위치를 띄웁니다.

- 환산 초점거리는 소니·니콘이 기록한 값을 그대로 쓰고, 그 태그를 아예
  쓰지 않는 캐논은 센서 크기에서 역산합니다(실측 오차 ±0.3%).
- AF 영역 모드는 캐논·니콘·소니에서 **실파일로 대조해 확인한 값만**
  이름을 붙입니다. 모르는 값은 추측하지 않고 줄을 생략합니다. 카메라
  용어라 번역하지 않습니다.
- 값이 없는 항목은 대시(—)가 아니라 **줄째 생략**합니다. 재저장 도구가
  EXIF를 깎아 낸 파일이 현장에 흔한데, 빈칸이 늘어서면 고장으로 읽힙니다.
- 위치는 소수 3자리(약 110m)까지만, **화면 표시로만** 씁니다. 내보내는
  파일에 GPS를 쓰지 않는 기존 방침은 그대로입니다.

값은 전부 분석 캐시에 이미 실려 있는 것이라 표시에 파일을 다시 읽지
않습니다.

### 카메라가 직접 뽑은 JPEG에서도 AF 위치를 읽습니다

지금까지 카메라의 AF 위치는 RAW에서만 읽었습니다. 확인해 보니 카메라가
직접 뽑은 JPEG에도 **같은 정보가 그대로** 들어 있고, 막혀 있던 것은 데이터가
아니라 확장자 목록이었습니다. **캐논·니콘 JPEG**을 읽습니다. 실측한 보관함
표본에서는 **75%가 판독 가능**했습니다(못 읽는 25%는 보정 프로그램이
내보내며 정보가 지워진 컷입니다).

구형 DSLR은 고정 AF 점 61·81개를 전부 싣고 초점이 맞은 점만 따로 표시하는
방식이라, 첫 점을 쓰면 엉뚱한 자리를 가리킵니다. 여러 점이 잡히면 그 점들을
감싸는 상자를 씁니다. **소니 JPEG은 아직 보류**입니다 — 실파일로 대조하지
못한 것을 넣으면 조용히 틀리기 때문입니다.

루페에서 **P** 키를 누르면 카메라가 기록한 AF 위치를 F(초점)·A(얼굴)·E(눈)
오버레이 옆에 함께 볼 수 있습니다.

### "주 피사체 불확실" 표시

카메라의 AF가 우리가 고른 주 피사체와 **다른 사람**을 가리키면 판정 근거에
"주 피사체 불확실" 한 줄을 답니다. **점수와 등급은 바꾸지 않습니다** — AF를
그대로 따르면 오히려 손해라(라벨 117장 실측: 갈렸을 때 우리 쪽이 87%,
AF 쪽이 49%) 선택을 바꾸는 데는 쓸 수 없고, 그 컷을 한 번 더 보시라는
신호로만 씁니다.

146,115장 전수 실측에서 **얼굴이 0~1명인 컷은 0%**(38,703장 전수)로
구조적으로 뜨지 않고, 2인 17% → 3인 30% → 6+인 47%로 늘어납니다. 웨딩·
포트레이트처럼 주 피사체가 하나뿐인 촬영에서는 도배되지 않습니다.

### 1인 구도 우선 (포트레이트)

분석 시작 창에 **1인 구도 우선 (포트레이트)**(기본 끔)이 생겼습니다. 켜면
주 피사체를 고를 때 **중앙에 있는지**를 먼저 봅니다.

주 피사체 선택의 최적 기준은 장르마다 다릅니다. 주인공을 한가운데 두는
포트레이트류에서는 중앙 우선이 47,990컷 홀드아웃에서 95.4%로 현행
77.9%를 크게 이기지만, 무대·그룹 촬영에서는 사람이 라벨한 110장에서
현행이 이깁니다(75.5% vs 68.2%). 어느 쪽인지는 찍은 사람만 아는 것이라
전역으로 바꾸지 않고 옵션으로 냅니다. 옵션마다 캐시가 따로 쌓이므로
되돌리면 예전 결과가 그대로 다시 나옵니다.

### 노이즈를 세게 걸면 패스가 자동으로 2가 됩니다

노이즈 감소 강도를 70보다 위로 올리면 **패스 수가 2로 함께 올라갑니다.**
같은 양을 지운다면 약하게 여러 번이 강하게 한 번보다 디테일을 훨씬 덜
다칩니다(실측 A6700 ISO3200, 노이즈 70% 제거에서 엣지 보존 1패스 74%
vs 2패스 97%). 슬라이더 값이 눈에 보이게 올라가므로 화면과 결과가 어긋나지
않고, 그대로 1로 되돌릴 수도 있습니다. 이미 3·4패스를 고르셨다면 건드리지
않습니다.

### 분석 버튼을 눌러도 옵션 창이 뜨지 않던 것을 고쳤습니다

0.15.2에서 들어온 분석 시작 창이 **한 번도 뜨지 않았습니다.** 버튼이 창을
건너뛰고 곧바로 분석을 시작하고 있었습니다. 그래서 캐시 무시, 노이즈 보정,
AF 위치 사용을 고를 방법이 사실상 없었고, 직전에 쓰던 설정 그대로만
분석됐습니다.

이제 툴바의 **분석**을 누르면 창이 뜹니다. 폴더를 열 때의 자동 분석은
전과 같이 묻지 않고 바로 시작합니다.

### 그 밖에 고친 것

- **맥용 DMG를 받아서 옮기면 "손상되었기 때문에 열 수 없습니다"가 뜨던
  것을 고쳤습니다.** 앱을 서명한 뒤에 번들 정보를 고쳐 서명이 깨져
  있었습니다. 만든 기계에서는 격리 표시가 없어 macOS가 전체 검사를
  건너뛰므로 멀쩡히 실행되고, 브라우저로 받은 사람만 막히는 종류의
  실패였습니다. 배포 이미지 형식도 APFS로 바꿨습니다 — 기존 형식은
  파일명을 다르게 저장해서, 번들에 든 한글 이름 프리셋이 복사되는 순간
  서명이 봉인해 둔 이름과 어긋났습니다. 이제 빌드가 DMG를 만든 뒤
  마운트해서 서명을 검사하고, 통과하지 못하면 배포본을 내놓지 않습니다.
  (공증은 여전히 없으므로 처음 열 때 "확인되지 않은 개발자" 안내는 뜹니다.)
- **맥에서 설정 → 정보 탭의 라이선스가 "파일을 찾지 못했습니다"로 뜨던
  것을 고쳤습니다.** 맥 배포본에만 `LICENSE`·`THIRD_PARTY.md`가 빠져
  있었습니다(윈도우는 담고 있었습니다). PySide6·libheif가 LGPL-3.0,
  pillow-heif가 GPL-2.0이라 고지가 반드시 함께 나가야 합니다.
  `CHANGELOG.md`도 같이 담습니다.
- **영어로 쓸 때 소요 시간만 한국어로 나오던 것을 고쳤습니다.** 분석 시작
  창과 캐시 비우기 확인 창에서 "800 photos to analyse — 약 3분"처럼 시간
  부분이 번역되지 않은 채 나왔습니다.
- **눈 감김 판정 임계를 0.30에서 0.25로 내렸습니다.** 사용자 라벨을 세 차례
  받아 다시 잡은 값입니다. 원인별로 정리한 라벨 152장에서 감은 컷의 67%를
  잡고 정확도는 79.4%입니다. 예전 0.30은 EAR 분포의 두꺼운 어깨에 앉아 있어,
  노래하거나 표정을 짓느라 반쯤 감긴 컷이 대량으로 걸렸습니다.
- **소니 AF 상자를 카메라가 실제로 표시한 크기로 그립니다.** 지금까지는
  AF 점만 읽고 긴 변의 8%로 상자를 합성했는데, 같은 자리에 실제 프레임
  크기가 들어 있었습니다(실측 A6700: 135×138). 판정 결과는 바뀌지 않고
  오버레이 정확도만 좋아집니다.
- **작은 버튼의 글자가 안 보이던 것을 고쳤습니다.** 슬라이더 되돌리기(↺),
  곡선의 RGB/R/G/B 채널, 색 휠 되돌리기처럼 폭이 고정된 작은 버튼에서
  라벨이 통째로 잘려 빈 사각형만 보였습니다.
- **분석 워커 수 계산이 파일 형식을 봅니다.** 장당 메모리를 하나의 상수로
  가정하고 있었는데 실측이 형식마다 크게 달랐습니다(NEF 385 / ARW 448 /
  CR3 525 / HEIF 1,225 / JPEG 1,347MB). RAW 폴더와 JPEG·HEIF 폴더에 각각
  맞는 예산을 씁니다 — 메모리가 작은 PC에서 JPEG·HEIF 폴더를 열 때 스왑에
  빠지지 않고, RAW 폴더에서 워커를 괜히 줄이지도 않습니다.

### 문서

영어 사용 설명서 **[docs/HOWTO.md](docs/HOWTO.md)** 를 새로 썼습니다. 열기 →
분석 → 셀렉트 → 현상 → 내보내기 순서로 전 기능을 덮고, 화면 그림 14장과
단축키 표가 들어 있습니다. 그림은 전부 합성 이미지로 찍었습니다.

`THIRD_PARTY.md`의 **pillow-heif 라이선스 기재를 정정**했습니다. Apache-2.0로
적어 두었으나 실제로는 **바이너리 휠이 GPL-2.0**입니다(x265 GPL-2.0을
동봉하기 때문이며, 휠 자신의 라이선스 파일에 그렇게 적혀 있습니다).
`.HIF`·`.HEIC`를 여는 데 쓰는 그 라이브러리이고, 실행 파일 배포본에도
실제로 들어 있습니다.

이 프로그램의 소스는 <https://github.com/SALB-247/raw-selector> 에 있습니다
(코드 MIT). 동봉한 다른 구성 요소의 조건과 출처는 `THIRD_PARTY.md`에
정리해 두었습니다.

### 올려 쓸 때 — 폴더를 처음 열 때 한 번 다시 분석합니다

분석 캐시 스키마가 **9**로 올라갔습니다(0.15.2는 6이었습니다). 촬영 정보
패널과 AF 신호가 쓰는 항목이 새로 들어갔기 때문입니다 — 올리지 않으면
예전 캐시가 빈 값으로 되살아나 패널 몇 줄이 영영 비어 있게 됩니다.

따로 하실 일은 없고, 이미 분석해 둔 폴더도 처음 한 번은 시간이 더 걸립니다.
썸네일과 내보내기 되돌리기 로그는 지워지지 않습니다. 위의 방향 수정도 이
재분석에서 함께 반영됩니다.

---

## 0.15.2 (2026-07-25)

### 분석 버튼에 시작 창이 생겼습니다

분석 버튼을 누르면 바로 시작하는 대신, **무엇을 몇 장 분석하는지** 먼저
보여 줍니다 — 사진 수, 캐시에서 재사용할 수 있는 장수, 새로 분석할 장수와
예상 시간. 여기서 세 가지를 고를 수 있습니다.

- **캐시 결과 사용** (기본 켬, 캐시가 없으면 잠김) — 끄면 캐시를 무시하고
  전부 다시 분석합니다. "설정을 바꾼 것도 아닌데 결과가 이상하다" 싶을 때
  쓰는 자리입니다.
- **노이즈에 강한 선명도** (기본 켬) — 선명도에서 노이즈 기여분을 빼는
  측정(초점판독 v4)을 켜고 끕니다. 예전 측정과 비교할 때만 끄십시오.
- **얼굴이 없을 때 카메라 AF 지점 사용** (기본 끔) — 얼굴을 못 찾은 컷에서
  "가장 선명해 보이는 타일"을 추측하는 대신, **카메라가 기록한 자동초점
  위치**를 판정 영역으로 씁니다. 소니 ARW·캐논 CR3·니콘 NEF(Z9의 HE/HE*
  포함)에서 읽습니다. 얼굴·눈이 있으면 항상 그쪽이 우선이라 인물 판정은
  변하지 않습니다. 판정 근거에는 "카메라 AF 지점"으로 표시됩니다.

폴더를 열 때의 자동 분석은 지금처럼 바로 시작합니다 — 이 창은 분석
버튼(다시 분석)에만 붙습니다. 옵션을 바꾸면 그 조합의 캐시가 따로
쌓이므로, 되돌리면 예전 결과가 그대로 다시 나옵니다.

### 파나소닉 RW2의 ISO와 렌즈가 나옵니다

RW2는 파일 형식 표식이 표준 TIFF와 달라 메타데이터 라이브러리가 파일째
거부하고 있었습니다. 모델·시각·초점거리는 내장 프리뷰에서 대신 읽어
왔지만 프리뷰에는 ISO·렌즈가 없어서 그 둘만 비어 있었습니다. 이제 파일
머리를 직접 읽어 채웁니다 (S1R 실측: ISO 12800, LUMIX S 24-105/F4 —
exiftool과 일치).

### 노이즈 감소 — 다중 패스, 고감도 보정, 어두운 곳 색 노이즈

콘서트처럼 ISO 2000 이상으로 찍는 촬영을 위한 세 가지입니다.

**패스 수 (세부 → 패스 수, 1~4, 기본 1).** 노이즈 감소를 여러 번, 매번
약하게 돌립니다. 같은 양을 지운다면 약한 여러 번이 강한 한 번보다
디테일을 훨씬 덜 다칩니다 — 실측(A6700 ISO3200)에서 노이즈를 70% 지울 때
1패스는 강한 엣지의 74%만 남기지만 3패스는 99%를 남깁니다. 80% 같은 깊은
감소는 1패스로는 아예 도달하지 못합니다(4패스는 85% 디테일로 도달).
시간은 패스 수에 비례하고, 비국소 평균 계열에서만 동작합니다(다른 방식을
고르면 잠깁니다).

**고감도에서 같은 슬라이더가 제 세기로 걸립니다.** 노이즈 자동 측정이
백색 노이즈만 가정해서, 디모자이크가 만드는 뭉친 노이즈를 고감도일수록
크게 놓치고 있었습니다(실측 5개 기종에서 22~35% 과소). 같은 슬라이더
값이 ISO가 높을수록 상대적으로 약해졌던 원인입니다. 보정 후 오차는
±14%로, 고감도 사진에서 같은 값이 예전보다 1.2~1.4배 세게 걸립니다.
저감도·노이즈 없는 사진은 변화가 없습니다.

**어두운 곳 색 노이즈 (세부 → 어두운 곳 색 노이즈, 0~100, 기본 0).**
색 얼룩은 섀도에서 특히 심한데, 전체 색 블러를 거기에 맞추면 밝은 곳의
진짜 색 경계까지 번집니다. 어두운 곳에만 더 센 블러를 섞습니다 —
실측(콘서트 3파일)에서 어두운 곳 잔여 색 노이즈가 25~29%에서 6~15%로
내려가고 밝은 곳은 완전히 그대로입니다.

### 노이즈가 초점 점수를 부풀리지 않습니다

**고감도의 노이즈 낀 소프트 컷이 선명 점수를 받던 문제를 고쳤습니다.**
선명도 측정에서 노이즈 기여분을 빼고 잽니다. 실측(5개 기종 11개 눈 ROI)
에서 노이즈가 선명도를 1.63배 부풀려 3개 ROI에서 '노이즈 낀 흐린 컷'이
'선명한 컷'을 이겼는데, 차감 후에는 역전이 없습니다.

실배치 검증(A6700 2846장): keep 수는 그대로(238장), 등급이 움직인 것은
28장(1.0%)뿐이고 대부분 어두운 고노이즈 연사가 review→reject로 강등된
것입니다 — 정확히 의도한 방향입니다. 분석 속도는 변화 없습니다.

**폴더를 처음 열 때 한 번 다시 분석합니다** (측정 알고리즘 버전이
올라갔습니다). 따로 하실 일은 없습니다.

---

## 0.15.1 (2026-07-23)

### 두 번째로 연 폴더에서 장수가 부풀던 것을 고쳤습니다

**한 번 분석한 폴더를 다시 열면 사진 수가 늘어났습니다.** 0.15에서 JPEG·HEIF도
판정 대상이 되면서, 프로그램이 자기 캐시 폴더(`.raw_selector_cache/thumbs`)에
만들어 둔 썸네일 JPEG까지 사진으로 세기 시작했습니다. 150장짜리 폴더를 두 번째
열면 300장이 되고, 장면 묶기와 keep 비율, `--target-keep`의 임계값이 통째로
어긋났습니다. 캐시 폴더를 목록에서 제외합니다.

같은 이유로 예전 이름(`.arw_selector_cache`)도 함께 제외합니다. 내보내기가 만든
`_keep`/`_review`/`_reject`는 이미 제외하고 있었습니다.

### 방향키로 슬라이더가 제대로 움직입니다

**색온도는 방향키를 눌러도 사실상 움직이지 않았습니다.** 방향키 한 번이
슬라이더 내부의 1칸이었는데, 그 1칸이 화면에서 얼마인지는 항목마다 달랐습니다.
색온도는 2000~12000K를 1K씩 움직여서 눈에 보이는 변화까지 100번을 눌러야 했고,
밝기는 같은 방향키가 1%를 움직였습니다.

이제 범위에 비례합니다 — 색온도 50K, 노출 0.05EV, 기울이기 0.5°. PageUp·PageDown은
그 열 배입니다. 이미 적당했던 항목(밝기, 브러시 크기 등)은 그대로입니다.

### 화면에서 고친 것

- **체크박스에 체크 표시가 없었습니다.** 켜지면 파란 네모로만 칠해져서 색 견본처럼
  보였습니다. 라디오 버튼은 가운데 점을 그려 주고 있었는데 체크박스만 빠져 있었습니다.
- **내보내기 버튼이 꺼져 있을 때 사라져 보였습니다.** 테두리가 없는 데다 배경색이
  툴바와 거의 같아서, 글자만 떠 있는 것처럼 보였습니다.

### 보정이 결과물에 제대로 걸립니다

**자동 렌즈 보정이 내보내기와 Full Render에서 빠졌습니다.** lensfun은 기종과
렌즈 이름으로 프로필을 찾는데, 그 정보를 정보 띠를 켰을 때만 읽고 있었습니다.
화면에서는 보정이 걸리는데 내보낸 파일에는 안 걸리고, 정보 띠를 켜 두면 그제서야
걸렸습니다 — 켜고 끄는 것과 아무 상관이 없어 보이는 조합입니다.

**기종 색 보정이 캐논 말고는 저장만 되고 다시 읽히지 않았습니다.** 쓸 때는
모델명만, 읽을 때는 제조사+모델로 파일 이름을 만들고 있었습니다. 둘이 같아지는
것은 모델명에 제조사가 이미 들어간 캐논뿐이라, 소니·니콘·파나소닉·후지는 계산을
끝내고 "저장했습니다"까지 뜬 뒤에도 색이 그대로였고 폴더를 열 때마다 계산을 다시
권했습니다. 이미 저장해 둔 값이 있으면 한 번 더 계산해야 합니다.

**JPEG·HIF에 렌즈 보정이 걸릴 수 있었습니다.** 화면에서 잠그는 것만으로는
막히지 않았습니다 — 잠긴 체크박스도 켜진 것으로 읽히고, 프리셋을 누르면 값이
되살아납니다. 이제 core에서 막으므로 프리셋·배치 어느 경로로 와도 같습니다.

**내보낸 사진의 EXIF 제조사가 항상 SONY였습니다.** 소니에서 출발한 도구라
박아 두었던 값입니다. 캐논 RAW를 내보내면 Make=SONY, Model=Canon EOS R6 Mark III
로 나갔습니다.

**이동으로 내보낸 뒤 되돌리면 현상본이 남았습니다.** 현상해서 새로 만든 파일까지
"원위치로 되돌릴" 대상으로 보다가, 원본이 이미 돌아와 있어 전부 실패로 끝났습니다.
새로 만든 것은 지우는 것이 맞습니다.

**보정 창에서 연 사진을 내보낼 때 아무것도 안 나가는 경우가 있었습니다.** 툴바
내보내기에서 등급을 좁혀 둔 적이 있으면 그 설정이 남아, 직접 열어 고른 사진이
등급 필터에 걸려 조용히 제외됐습니다.

### 분석 실패가 기록에 남습니다

**워커에서 난 실패가 로그 파일에 한 줄도 남지 않았습니다.** 분석은 자식 프로세스로
나뉘어 도는데, 그 프로세스들은 로그 설정을 물려받지 못합니다. 실패를 적어 두던 자리가
하필 그 안이라, 정작 남겨야 할 것만 사라졌습니다 — 창만 있는 배포본에서는 화면에도
안 보입니다. 이제 결과가 돌아오는 자리에서 파일명과 함께 남깁니다.

덧붙여 CR3·HIF를 열 때마다 exifread가 뱉던 "File format not recognized."를 잠급니다.
그쪽은 전용 파서로 따로 읽는 예상된 경로인데, HIF 2800장 폴더에서는 로그가 그
줄로만 찼습니다.

### macOS

Apple Silicon 맥에서 빌드하고 실행할 수 있습니다. `python build_macos.py --dmg`가
`.app` 번들과 배포용 `.dmg`를 만듭니다. 실행 하한은 macOS 14, Apple Silicon 전용입니다.

워커 수를 정할 때 보던 여유 메모리를 맥에서는 읽지 못하고 있었습니다(리눅스에만
있는 값을 쓰고 있었습니다). 코어 수만 보고 워커를 띄우던 것을, 메모리가 적은
맥에서도 스왑이 걸리지 않도록 다시 제한합니다.

자체 점검이 맥에서 "RAW_selector.exe 를 실행하시면 됩니다"라고 안내하던 것을 고쳤습니다.

---

## 0.15.0 (2026-07-23)

### JPEG·HIF만 있어도 됩니다

RAW를 안 찍는 분들을 위해, JPEG(`.jpg` `.jpeg`)과 HEIF(`.hif` `.heic` `.heif`)
파일도 판정하고 보정할 수 있게 했습니다. 폴더에 RAW가 하나도 없어도 됩니다.

**RAW가 있으면 RAW를 씁니다.** 카메라가 RAW+JPEG으로 같이 기록하면 같은 사진이
두 벌 있는데, 둘 다 목록에 넣으면 장수도 keep 비율도 통째로 두 배가 됩니다.
같은 폴더에 이름이 같은 RAW가 있으면 그 JPEG·HIF는 목록에서 빠집니다.

**감수해야 하는 것.** 이미 현상되어 8비트로 구워진 결과입니다. 날아간
하이라이트는 데이터 자체가 없어 돌아오지 않고, 큰 폭의 노출·색온도 조정에서
계조가 끊깁니다. 셀렉트와 가벼운 보정에는 충분합니다.

**보정 창에서 꺼지는 것.** 카메라 프로파일과 기종 색 보정, 렌즈 보정은 카메라가
이미 적용해 구워 넣었습니다. 한 번 더 걸면 이중 보정입니다. 그래서 적용도 안
하고 화면에서도 잠급니다 — 왜 잠겼는지는 패널에 적어 둡니다.

**색온도는 그대로 씁니다.** 센서 데이터가 없어 절대 Kelvin 값으로는 못 가지만,
상대적으로 따뜻하게·차갑게는 됩니다. 기본은 "손대지 않음"이라 열자마자 색이
변하지 않습니다.

**Full Render도 됩니다.** 프리뷰를 늘리는 것이 아니라 원본 해상도로 디코드해
화면 크기까지 줄입니다 (실측: HIF 4128×6192, 3.0초 — 디모자이크가 없어 짝
ARW의 6.6초보다 빠릅니다).

**내보내기에서 RAW 전용 옵션은 잠깁니다.** JPEG·HIF만 있는 배치에서는
"원본 RAW도 함께 내보내기"와 "함께 저장된 JPG/HIF/XMP도 내보내기"가 할 일이
없습니다. 잠그고 왜 못 쓰는지 적습니다. RAW가 한 장이라도 섞여 있으면
그대로 쓸 수 있습니다 — JPEG 쪽만 파일 단위로 건너뜁니다.

보정한 JPEG을 내보내면 현상본만 나갑니다. 원본까지 복사하면 이름도 형식도
같아서 `IMG_0001.jpg` 옆에 `IMG_0001_1.jpg`가 생기고 어느 쪽이 보정본인지
알 수 없습니다. 원본은 원래 폴더에 그대로 있습니다.

### HEIF 디코더가 필수 의존이 되었습니다

`.HIF`·`.HEIC`는 `pillow-heif`(안에 libheif)로만 열립니다 — OpenCV·Pillow·LibRaw
모두 거부합니다. 그래서 필수 의존으로 넣었습니다. libheif는 LGPL-3.0이라
PySide6와 같은 조건입니다(`THIRD_PARTY.md` 참고). `--selftest`가 이 디코더도
확인합니다.

---

## 0.14.0 (2026-07-22)

### 올려 쓰기 전에 — 해야 할 일

**1. 저장해 둔 판정 기준 프리셋을 다시 맞추십시오.**

점수 척도가 바뀌었습니다. 선명도 항이 예전의 절반이 되어(`scoring.SHARPNESS_SCALE`
= 0.5) 같은 사진의 점수가 전보다 낮게 나옵니다.

선명도만으로 0~100을 다 쓰면 보너스를 조금만 켜도 곧바로 100에 붙어, 잘 찍은
컷들이 전부 같은 점수가 되고 순위가 사라집니다. 셀렉터로서 가장 중요한 "이 중에
무엇이 나은가"를 못 하게 됩니다. 절반으로 눌러 선명도가 0~50을 쓰게 하고, 남은
50을 보너스·감점이 쓸 자리로 남겼습니다.

기본값도 함께 다시 잡았습니다. 척도만 반으로 줄인 것이 아니라, 실제 촬영본으로
맞춘 값으로 갈아 끼웠기 때문에 단순히 절반이 된 것이 아닙니다.

| 값 | 0.13 | 0.14 | |
|---|---|---|---|
| `keep_above` | 70 | 65 | 척도가 반이라 숫자보다 **훨씬 엄격해졌습니다** |
| `reject_below_group_best` | 20 | 10 | |
| `target_keep_ratio` | 기본으로 켜짐 | 없음 | 기본이 절대 점수 방식으로 바뀌었습니다 |

가중치·보너스·감점 기본값도 대부분 함께 움직였습니다. 판정 기준 패널에서
"기본값으로"를 누르면 새 값으로 맞춰집니다.

**직접 저장해 둔 프리셋과 손으로 쓴 `config.yaml`은 자동으로 옮겨지지 않습니다.**
예전 값을 그대로 불러오면 keep이 거의 나오지 않습니다.

목표 비율 모드(`target_keep_ratio`)를 쓰면 임계값을 배치 점수 분포에서 역산하므로
척도 변화의 영향을 받지 않습니다. 절대 점수를 다시 맞추기 번거로우면 이쪽을
쓰십시오.

**2. 폴더를 처음 열 때 한 번 다시 분석합니다.**

분석 캐시 스키마가 6으로 올라갔습니다(눈 감김 측정치가 추가됐습니다). 따로
하실 일은 없고, 이미 분석해 둔 폴더도 처음 한 번은 시간이 더 걸립니다.
썸네일과 내보내기 되돌리기 로그는 지워지지 않습니다.

**3. 배포본을 쓰신다면 `자체점검.bat`을 한 번 돌려 주십시오.**

점검 항목이 여섯에서 여덟으로 늘었습니다. 새로 추가된 "얼굴 윤곽 모델"이
[OK]로 나와야 아래의 마스크·눈 감김 기능이 실제로 동작합니다.

"판정 프리셋"도 새로 셉니다. 예전에는 보정 프리셋만 세면서 "기본 프리셋"이라고
표시해서, **판정 프리셋이 통째로 빠진 배포본이 점검을 통과했습니다.**

### 판정

- **눈 감김 감점.** 눈 감은 컷은 초점이 아무리 맞아도 못 쓰는데 선명도로는 전혀
  걸러지지 않았습니다. 주 피사체 얼굴에서 눈 종횡비(EAR)를 재어 감은 것으로
  보이면 감점하고, 판정 근거에 "눈 감김 의심"으로 표시합니다. 양쪽 눈 중 더 떠
  있는 쪽을 쓰고, 측정에 실패하면 감점하지 않습니다 — 못 잰 것과 감은 것은
  다릅니다. 감점은 작게 두었습니다. 목적은 reject로 떨어뜨리는 것이 아니라 자동
  keep에서 밀어내 review로 보내는 것입니다.
- **주 피사체 얼굴을 클릭해서 바꿉니다.** 미리보기에서 얼굴 오버레이를 켜고
  원하는 얼굴을 클릭하면 그 자리에서 초점을 다시 재고 점수와 판정 근거가
  갱신됩니다. 자동으로 고른 얼굴이 뒤에 선 사람인 경우가 있었습니다. 이 선택은
  그 세션에만 남습니다.
- **오버레이를 초점 / 얼굴 / 눈 셋으로 나눴습니다** (`F` / `A` / `E`). 초점
  영역만 보고 싶은데 얼굴 상자가 같이 나오면 정작 초점을 못 봅니다.
- **ROI 신뢰도 설정이 이제 실제로 반영됩니다.** 얼굴 우선 모드가 눈 0.85 /
  얼굴 0.75로 하한을 걸어, 그보다 낮게 내린 값은 **아무 반응이 없었습니다.**
  패널에서 눈 기준을 0으로 내려도 점수가 한 자리도 안 움직였습니다. 하한을
  없앴습니다 — 얼굴 우선 모드는 가감점으로만 작용합니다.
- **얼굴 보너스에 크기 가중이 붙었습니다** (판정 기준 → 보너스 → 보너스 기준
  얼굴 크기, 기본 3%). 얼굴 검출은 수십 화소짜리 얼굴도 찾아내서, 객석에 잡힌
  관객이 주 피사체로 뽑힌 컷이 인물을 크게 잡은 컷과 같은 가산을 받았습니다.
  기준보다 작은 얼굴은 크기에 비례해 덜 받습니다(면적의 제곱근 — 면적을
  그대로 쓰면 절반 크기 얼굴이 1/4로 떨어져 전신 인물컷까지 깎입니다).

  **망원으로 멀리서 찍는다면 기준을 내리십시오.** 300mm 무대 촬영 2845장에서
  주 피사체 얼굴은 중앙값이 화면의 0.34%, 최대가 2.99%였습니다. 그런 촬영이면
  0.3% 근처가 맞습니다. 0.5% 아래에서는 스핀박스가 0.1%씩 움직입니다.
- **채점표가 생겼습니다.** 격자에서 컷을 고르면 아래에 점수 내역이 펼쳐집니다 —
  선명도 항이 얼마고 어떤 보너스·감점이 몇 점씩 붙었는지, 얼굴 크기 배율이
  얼마나 깎았는지가 근거 수치와 함께 나옵니다. 판정 기준을 만지면 즉시
  다시 그려지므로 무엇이 움직였는지 보면서 맞출 수 있습니다.

  점수와 채점표는 **같은 계산**에서 나옵니다. 화면용으로 따로 더하면 규칙을
  고칠 때 한쪽만 고쳐서 화면과 실제 점수가 어긋납니다.
- **얼굴 우선 모드를 끄면 선명도 배수가 1.0이 되고, 얼굴·눈 항목은 전부
  꺼집니다.** 예전에는 모드를 꺼도 얼굴·눈 보너스가 그대로 붙었습니다 —
  "풍경 위주라면 꺼서 전체 프레임 선명도로만 판정합니다"라는 설명과 실제
  동작이 달랐습니다. 그러면서 배수는 0.5 그대로라, 얼굴 없는 촬영에서는
  점수의 상단 절반이 통째로 비었습니다(실측 A6700 2845장에서 최대 45.1점 —
  keep 기준 65에 닿는 컷이 하나도 없습니다).

  배수와 보너스는 같이 움직여야 합니다. 배수만 올리면 얼굴 보너스가 남아
  42장이 100점에 붙어 순위가 사라집니다.

  | 얼굴 우선 끔 | 평균 | 최대 | 100점 |
  |---|---|---|---|
  | 배수 0.5 · 보너스 그대로 (예전) | 33.7 | 68.0 | 0장 |
  | 배수 1.0 · 보너스 그대로 | 60.3 | 100.0 | **42장** |
  | 배수 1.0 · 보너스도 끔 (지금) | 53.2 | 90.2 | 0장 |

  판정 기준 패널에서도 모드를 끄면 해당 입력이 전부 잠기고, 공식 표시가
  바뀐 배수로 갱신됩니다. 살아 있는 위젯을 보면 그 값이 쓰인다고 믿게 되고,
  실제로는 반영되지 않으니 "바꿨는데 아무 일도 안 일어난다"가 됩니다.

  ROI 신뢰도와 노출 감점은 얼굴과 무관하므로 모드를 꺼도 그대로 쓰입니다.
- **눈을 뜬 컷에 가산이 붙습니다** (판정 기준 → 눈 상태 → 눈 뜸 보너스, 기본 10).
  감점만 있던 동안에는 "눈을 떴다"와 "눈을 못 쟀다"가 점수에서 완전히
  같았습니다. 옆얼굴이라 못 잰 원경이 정면으로 눈을 뜬 인물과 같은 대우를
  받은 셈이라, 인물 셀렉트에서 제일 중요한 신호가 반만 쓰이고 있었습니다.

  **못 잰 컷에는 여전히 아무것도 주지 않습니다** — 가산도 감점도 없습니다.
  모르는 것을 좋게도 나쁘게도 보지 않는다는 원칙은 그대로입니다.
- **눈 감김 임계 기본값이 0.30이 됐습니다** (예전 0.25). 사용자 라벨 107장으로
  다시 쟀습니다 — 잡아냄 / 거짓감점:

  | 임계 | 잡아냄 (감음 28) | 거짓감점 (뜸 79) | 감김 판정 중 맞은 비율 |
  |---|---|---|---|
  | 0.22 | 14 | 2 | 88% |
  | 0.25 | 17 | 7 | 71% |
  | 0.28 | 24 | 16 | 60% |
  | **0.30** | **25** | **19** | **57%** |
  | 0.32 | 25 | 26 | 49% |

  **0.30 위로는 올릴 이유가 없습니다** — 0.32는 잡는 수가 같은데 멀쩡한 컷만
  7장 더 깎습니다. 감음은 대부분 0.28 이하에 몰려 있고 뜬 눈은 0.20부터
  시작해서, 그 위에서는 두 분포가 겹치기만 합니다.

  올릴수록 "감김" 판정이 틀릴 확률이 올라간다는 점은 감안하십시오. 판정이
  맞는 쪽에 확실히 걸고 싶으면 0.25로 내리십시오.
- **눈 상태를 항상 알려 줍니다.** 예전에는 감점될 때만 "눈 감김 의심"이
  떴습니다. 그래서 "눈을 떴다", "감았지만 임계 위라 안 깎였다", "옆얼굴이라
  아예 못 쟀다"가 화면에서 전부 똑같이 아무 표시 없음으로 보였고, 눈 감긴
  컷을 사용자가 직접 찾아야 했습니다. 지금은 EAR 값과 임계를 같이 적습니다.
- **목표 keep 비율 모드에서 점수 분포를 보여 줍니다.** 장수·최소·평균·최대와
  "약 몇 점에서 잘리는지"가 비율 아래에 표시됩니다. 비율만 적혀 있으면 왜 이
  컷이 빠졌는지 설명할 방법이 없었습니다.

### 보정

- **얼굴·눈 마스크가 468점 윤곽 기반이 됐습니다.** 예전에는 눈 중심점 둘로
  사각형을 그려 이마와 볼까지 덮었고(같은 얼굴에서 화면의 4.09%), '이목구비
  제외'라던 피부 마스크에 눈·눈썹·입이 그대로 들어 있었습니다. 지금은 각각
  0.41% / 23.97%로, 눈은 눈꺼풀 윤곽만 피부는 이목구비를 실제로 뺀 부분만
  잡습니다.
- **치아 마스크가 생겼습니다** (프리셋 "치아 화이트닝"). 입술 *안쪽* 윤곽을
  씁니다 — 바깥 입술로 화이트닝을 걸면 입술까지 채도가 빠져 창백해집니다.
  입을 다문 컷에서는 효과가 없습니다. 보이지도 않는 치아를 만지지 않는 것이
  맞는 동작입니다.
- **방사형·선형 마스크를 이미지 위에서 끕니다.** 중심을 끌면 이동, 가장자리 점을
  끌면 크기, 바깥 점을 끌면 회전입니다. 선형은 시작점·끝점을 각각 끕니다.
  숫자로 위치를 맞출 필요가 없어졌습니다.
- **얼굴 검출기 초기화 경고를 지웠습니다.** OpenCV 5.0이 YuNet을 만들 때마다
  `setPreferableTarget Targets are not supported by the new graph engine`을
  찍었습니다. 실행 대상 힌트가 무시된다는 뜻일 뿐 검출은 정상입니다
  (실측 8장, 얼굴 개수 동일·주 얼굴 상자 0화소 차이). 다만 주 피사체를 바꿀
  때마다 콘솔에 쌓여서 정작 봐야 할 경고가 묻혔습니다. 검출기를 만드는
  구간에서만 막습니다 — 전역으로 낮추면 진짜 오류까지 사라집니다.
- **얼굴 영역 우선 노이즈 감소** (세부 → 얼굴 우선, 기본 85). 고감도에서 눈에
  거슬리는 것은 대개 피부의 알갱이인데, 같은 강도를 화면 전체에 걸면 옷의
  짜임·머리카락이 함께 뭉갭니다. 얼굴 밖에서는 강도를 덜어냅니다. 얼굴이
  검출되지 않으면 예전처럼 화면 전체에 같은 강도를 겁니다.

  **얼굴 밖을 0으로 만들지는 않습니다** — 우선도를 100까지 올려도 절반
  세기는 남습니다. 정작 노이즈가 심한 곳은 얼굴이 아니라 어두운 배경이라
  (실측 A6700 ISO3200: 얼굴 안 σ 1.52 / 얼굴 밖 σ 3.05), 거기를 통째로
  건너뛰면 노이즈 감소를 100으로 올려도 화면이 그대로였습니다.
- **노이즈 세기를 화면 전체 기준으로 잽니다.** 예전에는 실제로 계산하는
  영역에서만 쟀습니다. 얼굴 안은 대개 조용해서, 얼굴 우선을 켜면 같은
  슬라이더가 절반 세기로 동작했습니다 — "얼굴 우선을 켰더니 노이즈 감소가
  약해졌다"의 원인이었습니다.

  두 가지를 합쳐, 노이즈 감소 100 · 디테일 보존 50 · 얼굴 우선 100에서
  실측 결과가 이렇게 바뀌었습니다.

  | | 예전 | 지금 |
  |---|---|---|
  | 얼굴 안 노이즈 감소 | 57.6% | 64.0% |
  | 얼굴 밖 노이즈 감소 | 0.0% | 40.5% |

  대신 얼굴 밖도 계산하므로 느려집니다. 26MP 전면 비국소 평균이 약 2초입니다.
- **LED월 가로 줄무늬 제거** (세부 → 가로 줄무늬 제거, 기본 0/꺼짐). LED 패널의
  점멸과 롤링셔터 판독이 어긋나 생기는 가로 밴드를 지웁니다. 주기를 검출해
  그 성분만 빼므로, 줄무늬가 없는 사진에서 값을 올려도 아무 일도 하지 않습니다.
  풍경에서 하늘의 그라데이션을 건드릴 수 있어 기본은 꺼 두었습니다.

### 내보내기

- **TIFF 형식 추가.** JPEG / PNG / WebP / TIFF 넷이 됐습니다.
  HEIF/AVIF는 넣을 수 없습니다 — 지금 쓰는 OpenCV 빌드에 인코더가 없어 저장이
  실패합니다. RAW 옆의 `.HIF` 원본을 그대로 복사하는 것은 "함께 저장된
  JPG/HIF/XMP도 내보내기"로 됩니다.
- **장소별 폴더로 나누기 (GPS 기준).** 촬영 시각 순서대로 좌표를 훑어 반경 250m
  안에 이어지는 컷을 한 장소로 묶고, 장소 폴더 안에 등급 폴더를 만듭니다.
  폴더 이름은 좌표를 그대로 씁니다 — **온라인 지오코딩을 하지 않습니다.** 지명을
  붙이려면 촬영 위치를 외부 서버로 보내야 하는데, 사진에서 가장 민감한 정보를
  편의를 위해 흘릴 이유가 없습니다. GPS가 없는 컷은 `_위치없음`으로 갑니다.
  GPS를 기록하지 않는 카메라가 많아 기본은 꺼짐이고, 대화상자가 지금 배치에
  위치 정보가 있는 컷이 몇 장인지 먼저 알려 줍니다.

### 배포

- **얼굴 윤곽 모델이 빌드에 들어가지 않던 문제를 고쳤습니다.** 이 모델은 없어도
  예외를 던지지 않고 예전 방식으로 물러서기 때문에, 빌드도 실행도 성공한 채로
  마스크 정밀도와 눈 감김 감점만 조용히 빠져 있었습니다. 빌드 스크립트,
  배포 폴더 검증, 실행 파일 자체 점검 세 곳에서 모두 확인하도록 했습니다.
- 문서에 적힌 프리셋·로그 위치가 실제와 달랐습니다. 실제 위치는 **실행 파일 옆
  `data/`** 이고, 쓰기가 막힌 곳에 설치했을 때만 `%APPDATA%\raw_selector\data\`로
  물러섭니다.
- 이 CHANGELOG를 배포 폴더에 함께 넣습니다. "프리셋을 다시 맞추라" 같은 안내가
  받는 사람에게도 닿아야 하기 때문입니다.
- `verify_dist.py`의 **실행 확인 단계가 배포본을 실제로 돌려 본 적이 없었습니다.**
  cmd.exe에 넘기는 명령을 파이썬이 조립하면서 따옴표를 잘못 이스케이프해, exe가
  뜨지도 않고 경로 오류만 내고 끝났습니다. 이제 실제로 `--selftest`가 돕니다.

---

## 0.13.0 이전

이 파일을 만들기 전의 변경 이력은 남아 있지 않습니다.
